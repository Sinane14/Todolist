<!DOCTYPE html>
<html lang="fr" dir="ltr">
    <head>
        <meta charset="UTF-8">
    	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	    <Title>lé truk ke je doi fer</Title>
        <link rel="shortcut icon" href="{{ url_for('static', filename='Photo_1606890883800.png') }}">
        <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
    </head>
    <body>
        <div id="toptext">
            <H2>Blok naute 2 tash</H2>
            <div id="container">
            <div class="cadre">
                <h1>lé truk a fer</h1>
                    <strong>ajoute 1 truk a fer</strong>
                    <form action="{{ url_for('add') }}" method="POST" >
                        <input class="form-control" required type="text" name="todoitem" placeholder="Veuillez rentrer votre tache">
                        <input class="form-control" required type="date" name="finito" >

                        <input value="Ajouter une tache" type="submit" >
                    </form>
                    <div class="form-group">
                        <h2>pa terre mine nez</h2>
                            <div id="incomplete">
                                <ul>
                                    {% for todo in incomplete %}
                                        <li style="font-size: 30pt">{{ todo.text }}  C fini le {{ todo.finito }}  <button value="ilé fini ?"><a href="{{ url_for('complete', id=todo.id) }}">ilé fini ?</a></button><button><a href="{{ url_for('delete', id=todo.id) }}">Supprimer</a></button></li>
                                    {% endfor %}
                                </ul>
                            </div>
                        <h2>Terre mine nez</h2>
                            <div id="complete">
                                <ul>
                                    {% for todo in complete %}
                                        <li style="font-size: 30pt">{{ todo.text }} <button value="Truk a re fer ?"><a href="{{ url_for('incomplete', id=todo.id) }}">Truk a re fer ?</a></button><button><a href="{{ url_for('delete', id=todo.id) }}">Supprimer</a></button></li>
                                    {% endfor %}
                                </ul>
                            </div>
                    </div>
                </div>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" 
            integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ" crossorigin="anonymous">
        </script>
    </body>
</html>